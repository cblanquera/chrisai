# Feature Goals

Status: planning input
Last Updated:
Source:

This file captures high-level feature goals only. It is not an implementation
order, acceptance matrix, active progress queue, or instruction to keep all
future feature work inside the initial/root spec.

Before implementation, convert goals into bounded spec records, acceptance
criteria, and progress items. When a goal becomes a bounded product stream with
its own acceptance criteria, risks, decisions, POCs, or multiple implementation
items, create a sibling spec under `.agents/specs/` and add it to
`.agents/specs/manifest.md`.

When a user prompt identifies a new feature goal, append or reconcile it here as
planning input before deciding whether it belongs in an existing spec, a new
sibling spec, or active progress.

## Feature Goals

- <feature goal>
