interface:
  display_name: "ChrisAI Planning"
  short_description: "Routes large greenfield app planning before implementation"
  default_prompt: "Use $chrisai-planning when a large app has not been built yet and needs research-first planning, readiness review, MVP freeze, or implementation backlog generation before coding begins."
policy:
  allow_implicit_invocation: true
